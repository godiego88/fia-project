# Stage 1 Asset Universe Configuration
# Google Sheets is a MANUAL, READ-ONLY reference list.
# Edited only by humans.
# Used only to seed the Stage 1 securities universe.

source: google_sheets

google_sheets:
  sheet_id: "1OWThg74PwCsAz4_OpJZ8ZKykJCe0Oa8dXnT4LDAt_js"
  worksheet: "Universe"
  ticker_column: "ticker"

  # Hard guardrails
  min_universe_size: 5
  max_universe_size: 200

expectations:
  deduplicate: true
  uppercase: true
  sort: true
